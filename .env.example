##
# NutriPlan environment variables (template)
#
# Copy this file to `.env.local` and fill in real values.
# Never commit `.env.local` (it contains secrets).
##

# Prisma / Postgres
DATABASE_URL="postgresql://nutriplan_app:YOUR_PASSWORD@localhost:5432/nutriplan"

# Supabase (server-side)
SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"
SUPABASE_SERVICE_ROLE_KEY="YOUR_SUPABASE_SERVICE_ROLE_KEY"

# Supabase (client-side / browser)
# Should match the project used above, but uses NEXT_PUBLIC_* to be exposed to the browser bundle.
NEXT_PUBLIC_SUPABASE_URL="https://YOUR-PROJECT.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="YOUR_SUPABASE_ANON_KEY"

# Dev-only autologin (optional)
# Use seeded IDs from prisma/seed.ts if you want local auth without Supabase.
DEV_AUTOLOGIN="false"
DEV_USER_ID="00000000-0000-0000-0000-000000000101"
DEV_TENANT_ID="00000000-0000-0000-0000-000000000000"
DEV_ROLE="PATIENT"
